{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --minify --target=chrome100 --outfile=../wwwroot/tui-editor.bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@toast-ui/editor": "^3.2.2",
    "esbuild": "^0.28.0"
  }
}
